<div class="hero-unit">
	<h1>Lunchmatch</h1>

<br />
<br />
<br />


	<% if current_user %>

	<% if @set %>
		You have set up
	<% else %>
		<%= render 'lunchslots/activelunch' %>
	<% end %>

	<% else %>

		<%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %>

	<% end %>

</div>